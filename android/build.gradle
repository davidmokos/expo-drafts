plugins {
  id 'com.android.library'
  id 'expo-module-gradle-plugin'
}

dependencies {
  implementation project(':expo-updates')
}

group = 'expo.modules.drafts'
version = '0.1.0'

android {
  namespace "expo.modules.drafts"
  defaultConfig {
    versionCode 1
    versionName "0.1.0"
  }
  lintOptions {
    abortOnError false
  }
}
